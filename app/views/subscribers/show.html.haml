- model_class = Subscriber
.page-header
  %h1=t '.title', :default => model_class.model_name.human

%p
  %strong= model_class.human_attribute_name(:email) + ':'
  %br
  = @subscriber.email
%p
  %strong= model_class.human_attribute_name(:auth_token) + ':'
  %br
  = @subscriber.auth_token

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), subscribers_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_subscriber_path(@subscriber), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), subscriber_path(@subscriber), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
