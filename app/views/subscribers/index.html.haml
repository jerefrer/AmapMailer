- model_class = Subscriber
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:email)
      %th= model_class.human_attribute_name(:auth_token)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @subscribers.each do |subscriber|
      %tr
        %td= link_to subscriber.id, subscriber_path(subscriber)
        %td= subscriber.email
        %td= subscriber.auth_token
        %td=l subscriber.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_subscriber_path(subscriber), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), subscriber_path(subscriber), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_subscriber_path, :class => 'btn btn-primary'
