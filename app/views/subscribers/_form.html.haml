= simple_form_for @subscriber, :html => { :class => 'form-horizontal' } do |f|
  = f.input :email
  = f.association :categories, :as => :check_boxes, :label => 'Catégories'
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), subscribers_path, :class => 'btn'
