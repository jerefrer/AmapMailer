= simple_form_for @newsletter, html: { class: 'form-horizontal' } do |f|
  = f.input :name
  = f.association :categories, as: :check_boxes, label: "Catégories", required: true
  = f.input :body, input_html: { class: 'redactor' }
  = f.button :submit

= link_to t('.cancel', default: t("helpers.links.cancel")), admin_newsletters_path, class: 'btn'